#include <iostream>
#include <sstream>
#include <vector>

using namespace std;

/*
int main() {
    string s = "Insert(20,Đào Đình Hiếu,12A)";
    string subString = s.substr(7, s.length() - 8);
    cout << subString;
    cout << endl;
    stringstream ss(subString);
    vector<string> parts;
    string part;
    while (getline(ss, part, ',')) {
        parts.push_back(part);
    }
    for (string ele: parts) {
        cout << ele << " ";
    }
    return 0;
}
*/